# CAD inputs

Place candidate FCStd/STEP/DWG files here only with provenance and SHA-256 recorded in `ARIA-OBJECT-MAP.json`.

- Presence in this directory is not approval.
- Keep original source units and do not silently rescale.
- `ARIA-final-working.FCStd` may be created now from approved envelopes/placeholders. Replace them with scanned real assemblies/models as they become available without changing locked constraints.
- Never substitute `purchased-hardware/cad-review/01-...Official-Cooler...dwg` for the locked Pi/display/Smraza assembly.
- Do not commit large generated/export files here without checking repository policy.
