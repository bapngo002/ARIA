# CAD run reports

Store versioned scan, dimension verification, collision, optical, acoustic, airflow and assembly/removal reports here.

Every report identifies source commit, FCStd/input SHA-256, constraint version, run date, tool version and unresolved deviations. Do not overwrite evidence from an earlier release candidate.
